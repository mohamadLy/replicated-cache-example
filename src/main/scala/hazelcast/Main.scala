package hazelcast

/*
import akka.actor.ActorSystem
import com.hazelcast.core.Hazelcast
object Main extends App{

  val system = ActorSystem("distributed-cache-demo")

  Server.startServer
  /*
  val hazelcast = Hazelcast.newHazelcastInstance()

  val map: java.util.Map[String, Employee] = hazelcast.getMap("customer")

  map.put("1", Employee("1", "John", "Doe"))
  map.put("2", Employee("2", "Mike", "Doe"))

  val values = map.values()

  println(values)
   */
//}


//case class Employee(id: String, firstName: String, lastName: String)
